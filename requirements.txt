
speedtest-cli==2.1.3     # Network speed test

schedule==1.2.1          # Schedule tasks

gtts==2.3.2              # Text to Speech (automated voice call)

selenium==4.20.0         # Auto post to Facebook (if not using Facebook API)

yagmail==0.15.293        # Easy email sending (SMTP wrapper)

twilio==9.0.3            # (Optional) Make calls via API

